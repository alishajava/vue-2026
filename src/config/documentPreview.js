// pptx를 어떻게 미리보기 렌더링할지 정하는 스위치.
//
// true (현재값): .ppt와 똑같이 서버(Apache POI)가 슬라이드를 PNG로 변환해서 내려주고,
// 프론트는 그 이미지 배열을 그대로 보여준다. pptx-preview 라이브러리를 아예 안 쓰기
// 때문에, 이번 세션에서 계속 나왔던 렌더링 버그(자리표시자 텍스트, 검은 줄, 테두리
// 안 보임 등)에서 자유롭다. 대신 미리보기를 열 때마다 서버 변환이 필요하다
// (파일 올리기 전 즉시 미리보기가 실제 요건이 아니라서 이 트레이드오프를 받아들이기로 함).
//
// false로 바꾸면 DocumentSlideViewerModal.vue에 그대로 남아있는 기존 pptx-preview
// 기반 클라이언트 렌더링 로직(백업으로 남겨둠, 삭제하지 않음)으로 즉시 되돌아간다.
export const PPTX_USE_SERVER_CONVERSION = true
